<Window x:Class="AeroFleetManagerPro.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:AeroFleetManagerPro.Converters"
        xmlns:views="clr-namespace:AeroFleetManagerPro.Views"
        mc:Ignorable="d"
        Title="{Binding Title}" 
        Height="800" Width="1400"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Border Grid.Row="0" 
                                  Background="#004445" 
                                  Padding="16">
            <DockPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="✈" 
                                           FontSize="30" 
                                           Foreground="White"
                                           VerticalAlignment="Center"/>
                    <TextBlock Text="AeroFleet Manager Pro - Управління авіафлотом" 
                             VerticalAlignment="Center"
                             Margin="10,0,0,0" 
                             FontSize="20" 
                             Foreground="White"
                             FontWeight="Medium"/>
                </StackPanel>
                
                <StackPanel Orientation="Horizontal" 
                          HorizontalAlignment="Right">
                    <Button Background="#2C7873" Foreground="White" BorderThickness="1" BorderBrush="#6FB98F" Padding="10,5"
                          Command="{Binding NavigateToDashboardCommand}"
                          Margin="5,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📊" Margin="0,0,5,0"/>
                            <TextBlock Text="Панель"/>
                        </StackPanel>
                    </Button>
                    
                    <Button Background="#2C7873" Foreground="White" BorderThickness="1" BorderBrush="#6FB98F" Padding="10,5"
                          Command="{Binding NavigateToFleetCommand}"
                          Margin="5,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="✈" Margin="0,0,5,0"/>
                            <TextBlock Text="Флот"/>
                        </StackPanel>
                    </Button>
                    
                    <Button Background="#2C7873" Foreground="White" BorderThickness="1" BorderBrush="#6FB98F" Padding="10,5"
                          Command="{Binding NavigateToAnalyticsCommand}"
                          Margin="5,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📈" Margin="0,0,5,0"/>
                            <TextBlock Text="Аналітика"/>
                        </StackPanel>
                    </Button>
                    
                    <Button Background="#2C7873" Foreground="White" BorderThickness="1" BorderBrush="#6FB98F" Padding="10,5"
                          Command="{Binding NavigateToPlanningCommand}"
                          Margin="5,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📅" Margin="0,0,5,0"/>
                            <TextBlock Text="Планування"/>
                        </StackPanel>
                    </Button>
                    
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" 
                                Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="Швидкі дії" 
                             Margin="15,15,15,10" 
                             FontSize="16" 
                             FontWeight="Medium"/>
                    
                    <Separator Margin="0,0,0,10"/>
                    
                    <Button Background="Transparent" Foreground="White" BorderThickness="0" Padding="10,5"
                          HorizontalAlignment="Stretch"
                          Margin="10,2"
                          Command="{Binding AddAircraftCommand}"
                          ToolTip="Додати новий літак">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="+" Margin="0,0,10,0"/>
                            <TextBlock Text="Додати літак"/>
                        </StackPanel>
                    </Button>
                    
                    <Button Background="Transparent" Foreground="White" BorderThickness="0" Padding="10,5"
                          HorizontalAlignment="Stretch"
                          Margin="10,2"
                          Command="{Binding AddFlightCommand}"
                          ToolTip="Додати новий рейс">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📅+" Margin="0,0,10,0"/>
                            <TextBlock Text="Запланувати рейс"/>
                        </StackPanel>
                    </Button>
                    
                    
                    <Separator Margin="0,10"/>
                    
                    <TextBlock Text="Стан системи" 
                             Margin="15,10,15,10" 
                             FontSize="16" 
                             FontWeight="Medium"/>
                    
                    <StackPanel Margin="15,0,15,15">
                        <TextBlock>
                            <Run Text="База даних:" FontWeight="Medium"/>
                            <Run Text="Підключено" Foreground="Green"/>
                        </TextBlock>
                        
                        <TextBlock Margin="0,5,0,0">
                            <Run Text="Останнє оновлення:" FontWeight="Medium"/>
                            <Run Text="активна" />
                        </TextBlock>
                    </StackPanel>
                </StackPanel>
            </Border>

            <ContentControl Grid.Column="1" 
                          Content="{Binding CurrentViewModel}"
                          Margin="5">
                <ContentControl.Resources>
                    <DataTemplate DataType="{x:Type viewmodels:DashboardViewModel}"
                                xmlns:viewmodels="clr-namespace:AeroFleetManagerPro.ViewModels">
                        <views:DashboardView/>
                    </DataTemplate>
                    <DataTemplate DataType="{x:Type viewmodels:FleetViewModel}"
                                xmlns:viewmodels="clr-namespace:AeroFleetManagerPro.ViewModels">
                        <views:FleetView/>
                    </DataTemplate>
                    <DataTemplate DataType="{x:Type viewmodels:AnalyticsViewModel}"
                                xmlns:viewmodels="clr-namespace:AeroFleetManagerPro.ViewModels">
                        <views:AnalyticsView/>
                    </DataTemplate>
                    <DataTemplate DataType="{x:Type viewmodels:PlanningViewModel}"
                                xmlns:viewmodels="clr-namespace:AeroFleetManagerPro.ViewModels">
                        <views:PlanningView/>
                    </DataTemplate>
                    <DataTemplate DataType="{x:Type viewmodels:MaintenanceViewModel}"
                                xmlns:viewmodels="clr-namespace:AeroFleetManagerPro.ViewModels">
                        <views:MaintenanceView/>
                    </DataTemplate>
                </ContentControl.Resources>
            </ContentControl>
        </Grid>

        <StatusBar Grid.Row="2" Height="30">
            <StatusBarItem>
                <TextBlock Text="AeroFleet Manager Pro - Система управління авіаційним флотом"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock Text="AeroFleet Manager Pro v1.0"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>